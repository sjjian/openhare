name: sql_complete
description: SQL completion with sql_parser tokenization and a pure-Dart causal attention model.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.2.1

dependencies:
  sql_parser:
    path: ../sql_parser/

dev_dependencies:
  lints: ^2.1.0
  test: ^1.25.2
